import React, { Component } from "react";
import { hot } from 'react-hot-loader';

const AppTest = (props)=>{
    return(
      <div>
        
      </div>
    )
}

export default hot(module)(AppTest);
